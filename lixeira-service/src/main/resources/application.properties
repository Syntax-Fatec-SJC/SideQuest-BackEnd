# Configuração da Aplicação
spring.application.name=lixeira-service
server.port=8085
# Spring Cloud Compatibility
spring.cloud.compatibility-verifier.enabled=false

# MongoDB - Mesma conexão
spring.data.mongodb.uri=mongodb+srv://syntax:fatec123@syntax-bd.vulmuug.mongodb.net/syntaxbd?retryWrites=true&w=majority&appName=syntaxbd
spring.data.mongodb.database=syntaxbd

# JWT Configuration (mesma do API Gateway)
jwt.secret=SideQuestSecretKey2024ForJWTAuthenticationAndAuthorizationSystem
jwt.expiration=86400000

# URLs dos outros microserviços
projetos.service.url=http://localhost:8083
tarefas.service.url=http://localhost:8084

# Gateway Secret
gateway.secret=SideQuestSecretKey2024ForJWTAuthenticationAndAuthorizationSystem

# Lixeira Configuration
lixeira.dias-retencao=30
lixeira.cron-limpeza=0 0 2 * * ?

# CORS
cors.allowed-origins=http://localhost:5173,http://localhost:8080

# Swagger/OpenAPI Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.packages-to-scan=com.syntax.lixeira_service.controller

# Actuator
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# Logging
logging.level.root=INFO
logging.level.com.syntax=DEBUG
